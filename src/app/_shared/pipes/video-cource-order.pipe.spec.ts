import { VideoCourceOrderPipe } from './video-cource-order.pipe';

describe('VideoCourceOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new VideoCourceOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
