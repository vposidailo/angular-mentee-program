import { CreationDateDirective } from './creation-date.directive';

describe('CreationDateDirective', () => {
  it('should create an instance', () => {
    const directive = new CreationDateDirective(null);
    expect(directive).toBeTruthy();
  });
});
